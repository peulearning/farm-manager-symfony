<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Farm Manager{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-success mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('relatorios_inicial') }}">Farm Manager</a>
        <div>
             <a href="{{ path('relatorios_inicial') }}" class="btn btn-outline-dark btn-sm">🏠 Início</a>
             <a href="{{ path('gados_index') }}" class="btn btn-outline-dark btn-sm">🐄 Gados</a>
             <a href="{{ path('fazendas_index') }}" class="btn btn-outline-dark btn-sm">🏡 Fazendas</a>
             <a href="{{ path('veterinario_index') }}" class="btn btn-outline-dark btn-sm">👨‍⚕️ Veterinários</a>
        </div>
    </div>
</nav>

<div class="container">
    {% include 'partials/_flash_messages.html.twig' %}
    {% block body %}{% endblock %}
</div>

</body>
</html>
