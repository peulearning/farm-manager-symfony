{% extends 'base.html.twig' %}
{% block title %}Editar Fazenda{% endblock %}
{% block body %}
<h1>Editar Fazenda #{{ fazenda.id }}</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning mt-3">Salvar Alterações</button>
{{ form_end(form) }}

<a href="{{ path('fazendas_index') }}" class="btn btn-secondary mt-3">Voltar</a>
{% endblock %}
